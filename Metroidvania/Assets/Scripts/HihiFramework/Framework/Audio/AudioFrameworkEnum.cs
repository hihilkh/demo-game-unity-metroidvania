namespace HihiFramework.Audio {
    public static class AudioFrameworkEnum {
        public enum Category {
            Bgm,
            Sfx,
        }

        public enum VolumeScale {
            Decibel,
            Linear,     // SoundIntensity
        }
    }
}