public class TutorialEnum {
    // Remarks: Never change the GameTutorialType enum int value. It is used in map json data.
    public enum GameTutorialType : int {
        StartWithConfusion = 0,       // The default value 0 is used for any fallback cases
    }
}